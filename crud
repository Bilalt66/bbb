<?php

$servername = "localhost";
$username = "root";
$password = "";
$dbname = "mijn_database";

try {
    $conn = new PDO("mysql:host=$servername;dbname=$dbname", $username, $password);
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);

    echo "Connected successfully<br><br>"; // 

} catch (PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

if (isset($_POST['add'])) {
    $conn->exec("INSERT INTO producten (naam, prijs) VALUES ('$_POST[naam]', '$_POST[prijs]')");
}

if (isset($_GET['del'])) {
    $conn->exec("DELETE FROM producten WHERE id=$_GET[del]");
}

if (isset($_POST['upd'])) {
    $conn->exec("UPDATE producten SET naam='$_POST[naam]', prijs='$_POST[prijs]' WHERE id=$_POST[id]");
}

$q = $conn->query("SELECT * FROM producten");
?>

<h3>Toevoegen</h3>
<form method="post">
    <input name="naam">
    <input name="prijs">
    
    <button name="add">+</button>
</form>

<h3>Lijst</h3>

<?php foreach ($q as $r) { ?>
    <form method="post">
        <input type="hidden" name="id" value="<?= $r['id'] ?>">
        <input name="naam" value="<?= $r['naam'] ?>">
        <input name="prijs" value="<?= $r['prijs'] ?>">
        <button name="upd">update</button>
        <a href="?del=<?= $r['id'] ?>">x</a>
    </form>
<?php } ?>